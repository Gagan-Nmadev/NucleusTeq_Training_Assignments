# Q2. Use map() to convert a list of numbers into their squares.

print("Q2. Use map() to convert a list of numbers into their squares.")

squares = map(lambda x: x * x, [2, 8, 9, 10])
print(next(squares))
print(next(squares))
print(next(squares))
print(next(squares))